global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: moodfood-backend
    static_configs:
      - targets:
          # Dev mode: backend runs on host (npm run dev)
          - host.docker.internal:3000
    metrics_path: /metrics
