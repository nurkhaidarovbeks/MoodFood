## What this PR does
[Describe the changes in 2-3 sentences]

## How to test
1. `docker-compose up -d` (or `npm run dev` in `Backend/`)
2. Go to the changed endpoint/page
3. Verify expected behavior

## Screenshots / Evidence
[Attach screenshots, Postman responses, or logs if relevant]

## Checklist
- [ ] Works locally (`npm run dev`)
- [ ] All tests pass (`npm test`)
- [ ] TypeScript compiles without errors (`npm run build`)
- [ ] `.env` is NOT committed
- [ ] No hardcoded passwords, tokens, or secrets
- [ ] Reviewed by at least one teammate
